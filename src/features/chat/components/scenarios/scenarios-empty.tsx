import {EmptyView} from "@/components/entity-components";

export default function ScenariosEmpty() {
    return <EmptyView message="Keine Szenarien gefunden. Versuche es mit einer anderen Suche." />
}

